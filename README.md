# schema

Package schema implements various subsets of [RFC4512](https://www.rfc-editor.org/errata/rfc4512) to facilitate an LDAP schema definition parser.

Also included is a fast map-based schema indexer. See the `indexer` package subdirectory for details.
